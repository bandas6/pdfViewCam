import { SanearURLPipe } from "./sanear-url.pipe";


describe('SanearURLPipe', () => {
  it('create an instance', () => {
    const pipe = new SanearURLPipe();
    expect(pipe).toBeTruthy();
  });
});
