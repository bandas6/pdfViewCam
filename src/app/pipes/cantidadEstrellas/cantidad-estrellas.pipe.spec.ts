import { CantidadEstrellasPipe } from './cantidad-estrellas.pipe';

describe('CantidadEstrellasPipe', () => {
  it('create an instance', () => {
    const pipe = new CantidadEstrellasPipe();
    expect(pipe).toBeTruthy();
  });
});
