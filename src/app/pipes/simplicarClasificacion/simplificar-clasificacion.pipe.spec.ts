import { SimplificarClasificacionPipe } from './simplificar-clasificacion.pipe';

describe('SimplificarClasificacionPipe', () => {
  it('create an instance', () => {
    const pipe = new SimplificarClasificacionPipe();
    expect(pipe).toBeTruthy();
  });
});
